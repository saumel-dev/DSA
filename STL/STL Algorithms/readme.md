STL containers store data; STL algorithms perform common operations on that data.

STL algorithms generally operate on a range defined by iterators, rather than relying on container-specific indexes.

STL algorithms need to know the beginning and the end of the range on which they should operate.

